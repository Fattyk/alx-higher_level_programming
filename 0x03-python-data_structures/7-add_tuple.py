#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    result = ()
    first = turple_a + (0, 0)
    second = turple_b + (0, 0)
    result = first[0] + second[0], first[1] + second[1]
    return result
